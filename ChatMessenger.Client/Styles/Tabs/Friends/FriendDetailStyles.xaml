<!-- FriendDetailView에서 사용되는 Style들을 정의하는곳 -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <!-- 프로필 우측 상단 더보기 버튼 -->
    <!-- 우측 상단 더보기 탭 Style -->
    <Style x:Key="FriendDetail_SeeMorePopupBox" TargetType="materialDesign:PopupBox" BasedOn="{StaticResource MaterialDesignPopupBox}">
        <Setter Property="PlacementMode" Value="BottomAndAlignRightEdges"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="10,10"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Visibility" Value="{Binding CurrentFriend.IsMe, Converter={StaticResource InvertedBoolToHidden}}"/>
    </Style>
    <!-- 즐겨찾기 버튼 StackPanel -->
    <Style x:Key="FriendDetail_FavoriteButton_StackPanel" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Vertical"/>
        <!-- 친구 추가상태여야 보이게 설정 -->
        <Setter Property="Visibility" Value="{Binding CurrentFriend.IsAdded, Converter={StaticResource BoolToVis}}"/>
    </Style>
    <!-- 친구 등록, 삭제 버튼 StackPanel -->
    <Style x:Key="FriendDetail_FriendButton_StackPanel" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Vertical"/>
        <!-- 차단 안돼있어야 보이게 설정 -->
        <Setter Property="Visibility" Value="{Binding CurrentFriend.IsBlocked, Converter={StaticResource InvertedBoolToColl}}"/>
    </Style>

    <!-- 프로필 이미지 수정 Button -->
    <Style x:Key="FriendDetail_ProfileChangeButton" TargetType="Button" BasedOn="{StaticResource Common_ProfileChangeButton}">
        <Setter Property="Visibility" Value="{Binding CurrentFriend.IsMe, Converter={StaticResource BoolToVis}}"/>
    </Style>
    <!-- 친구 정보 표시 StackPanel Style-->
    <Style x:Key="FriendDetail_Info_StackPanel" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Vertical"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Visibility" Value="{Binding IsEditMode, Converter={StaticResource InvertedBoolToColl}}"/>
    </Style>
    <!-- 기본 정보 표시 StackPanel -->
    <Style x:Key="FriendDetail_NicknameStackPanel" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,20"/>
    </Style>
    <!-- 닉네임 표시용 TextBlock -->
    <Style x:Key="FriendDetail_Info_NicknameTextBlock" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>
    <!-- 이메일 표시용 TextBlock -->
    <Style x:Key="FriendDetail_Info_EmailTextBlock" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Margin" Value="5,0,0,0"/>
    </Style>
    <!-- 상태 메세지 표시용 TextBlock -->
    <Style x:Key="FriendDetail_Info_StatusMessageTextBlock" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- 프로필 수정용 Stack Panel -->
    <Style x:Key="FriendDetail_Edit_StackPanel" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Vertical"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Visibility" Value="{Binding IsEditMode, Converter={StaticResource BoolToVis}}"/>
    </Style>
    <!-- 닉네임 수정용 TextBox -->
    <Style x:Key="FriendDetail_Edit_NicknameTextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="materialDesign:TextFieldAssist.HasClearButton" Value="True"/>
        <Setter Property="materialDesign:HintAssist.Hint" Value="Nickname"/>
        <Setter Property="MaxLength" Value="12"/>
        <Setter Property="materialDesign:TextFieldAssist.CharacterCounterVisibility" Value="Visible"/>
        <Setter Property="Width" Value="150"/>
    </Style>
    <!-- 수정 패널의 경고 메세지 Style -->
    <Style x:Key="FriendEtail_Edit_WarningText" TargetType="TextBlock" BasedOn="{StaticResource Common_WarningTextBlock}">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,100,5"/>
    </Style>
    <!-- 상태 메세지 수정용 TextBox -->
    <Style x:Key="FriendDetail_Edit_StatusMessageTextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="materialDesign:TextFieldAssist.HasClearButton" Value="True"/>
        <Setter Property="materialDesign:HintAssist.Hint" Value="Status Message"/>
        <Setter Property="MaxLength" Value="30"/>
        <Setter Property="materialDesign:TextFieldAssist.CharacterCounterVisibility" Value="Visible"/>
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="Width" Value="350"/>
        <Setter Property="Height" Value="100"/>
    </Style>

    <!-- 내 프로필일때 최하단 수정 버튼 표시 Panel -->
    <Style x:Key="FriendDetail_EditButtonPanel" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,25"/>
        <Setter Property="Visibility" Value="{Binding CurrentFriend.IsMe, Converter={StaticResource BoolToHidden}}"/>
    </Style>
</ResourceDictionary>