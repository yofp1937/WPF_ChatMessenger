<UserControl
    x:Class="ChatMessenger.Client.Views.RegisterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:converters="clr-namespace:ChatMessenger.Client.Common.Converters">
    
    <UserControl.Resources>
        <converters:MultiBindingConverter x:Key="MultiBindingConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <materialDesign:Card Style="{StaticResource MDStyle_Card}">
                <StackPanel>
                    <Grid>
                        <Button ToolTip="Back to Login" Style="{StaticResource Common_GoBackButton}"
                                    Command="{Binding MoveToLoginCommand}"/>
                        <TextBlock Text="Register" Style="{StaticResource Common_TitleTextBlcok}"/>
                    </Grid>

                    <TextBox Text="{Binding Email}" materialDesign:HintAssist.Hint="Email" Margin="0,10"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    
                    <TextBox Text="{Binding Nickname}" materialDesign:HintAssist.Hint="Nickname" Margin="0,10"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                    <PasswordBox x:Name="PasswordInput" materialDesign:HintAssist.Hint="Password" Margin="0,10"
                                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"/>

                    <PasswordBox x:Name="PasswordInputCheck" materialDesign:HintAssist.Hint="Confirm Password" Margin="0,10"
                                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"/>
                    
                    <TextBlock Text="{Binding WarningText}" Style="{StaticResource Common_WarningTextBlcok}"/>

                    <Button Content="Create Acount" Style="{StaticResource Common_RequestButton}"
                                Command="{Binding RegisterCommand}">
                        <Button.CommandParameter>
                            <MultiBinding Converter="{StaticResource MultiBindingConverter}">
                                <Binding ElementName="PasswordInput"/>
                                <Binding ElementName="PasswordInputCheck"/>
                            </MultiBinding>
                        </Button.CommandParameter>
                    </Button>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </Grid>
</UserControl>