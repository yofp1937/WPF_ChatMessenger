<UserControl
    x:Class="ChatMessenger.Client.Views.Tabs.Chats.ChatListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- 검색 TextBox -->
        <DockPanel>
            <Button Command="{Binding CreateChatRoomCommand}" Style="{StaticResource ChatList_CreateChatBtn}"/>
            <TextBox Text="{Binding SearchRoomText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ChatList_RoomSearchTextBox}"/>
        </DockPanel>
        
        <!-- 경계선 -->
        <Separator Grid.Row="1" Style="{StaticResource Common_Boundary}"/>
        
        <!-- 채팅방 ListBox -->
        <ListBox Grid.Row="2" Style="{StaticResource Common_ListViewsListBox}"
                    ItemsSource="{Binding ChatRooms}"  SelectedItem="{Binding SelectedRoom, Mode=TwoWay}"
                    ItemTemplate="{StaticResource ChatList_ChatRoomTemplate}"/>
    </Grid>
</UserControl>