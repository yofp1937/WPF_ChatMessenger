<Window
    x:Class="ChatMessenger.Client.Views.MainWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    
    Title="ChatMessenger" Height="600" MinHeight="400" Width="1000" MinWidth="800"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    AllowsTransparency="True"
    Style="{StaticResource MaterialDesignWindow}"
    Background="{DynamicResource MaterialDesignPaper}"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 메인 타이틀바 -->
        <materialDesign:ColorZone Grid.Row="0" Style="{StaticResource MainTitleBarColorZone}">
            <DockPanel LastChildFill="False">
                <!-- 왼쪽 아이콘, 프로그램 이름 -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <materialDesign:PackIcon Style="{StaticResource MainTitleBarPackIcon}"/>
                    <TextBlock Style="{StaticResource MainTitleBarText}" Text="CHAT MESSENGER"/>
                </StackPanel>
                <!-- 오른쪽 최소화, 최대화, 닫기 버튼 -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Style="{StaticResource MainTitleBarMinimizeButton}" Command="{Binding MinimizeWindowCommand}"/>
                    <Button Style="{StaticResource MainTitleBarMaximizeButton}" Command="{Binding MaximizeWindowCommand}"/>
                    <Button Style="{StaticResource MainTitleBarCloseButton}" Command="{Binding CloseWindowCommand}"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>
        
        <!-- 메인 화면 -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentView}" />
    </Grid>
</Window>
