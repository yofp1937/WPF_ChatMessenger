<UserControl
    x:Class="ChatMessenger.Client.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <materialDesign:Card Style="{StaticResource MDStyle_Card}">
                <StackPanel>
                    <TextBlock Text="LOGIN" Style="{StaticResource Common_TitleTextBlcok}"/>

                    <TextBox Text="{Binding Email}" materialDesign:HintAssist.Hint="Email" Margin="0,10"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                    <PasswordBox x:Name="PasswordInput" materialDesign:HintAssist.Hint="Password" Margin="0,10"
                                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"/>

                    <TextBlock FontSize="12" Margin="0,0,0,5">
                        <Run Text="Need an acount?" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <Hyperlink Command="{Binding MoveToRegisterCommand}" Foreground="DodgerBlue">
                            <Run Text="Register here" FontWeight="Bold"/>
                        </Hyperlink>
                    </TextBlock>

                    <TextBlock Text="{Binding WarningText}" Style="{StaticResource Common_WarningTextBlcok}"/>

                    <Button Content="SIGN IN" Style="{StaticResource Common_RequestButton}"
                                Command="{Binding SignInCommand}"
                                CommandParameter="{Binding ElementName=PasswordInput}"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </Grid>
    
</UserControl>